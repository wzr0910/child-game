# 技术架构

> 本文件描述**当前已实现**的架构。带 ✅ 的为已落地，带 ⏳ 的为规划/未接入，避免与 PRD 的远期规划混淆。

---

## 1. 整体架构图

```
┌─────────────────┐
│   用户浏览器     │
└────────┬────────┘
         │ HTTPS
         ▼
┌─────────────────────────────────────┐
│        Next.js (前端 + 后端一体)     │
│  ┌─────────────┐  ┌──────────────┐ │
│  │  React 页面  │  │ API Routes   │ │
│  │  (App Router)│  │ /api/chat    │ │
│  │             │  │ /api/generate │ │
│  │             │  │ /api/gallery │ │
│  └──────┬──────┘  └──────┬───────┘ │
└─────────┼────────────────┼─────────┘
          │                │
          ▼                ▼
   ┌──────────────┐  ┌──────────────┐
   │  DeepSeek    │  │  CloudBase    │
   │  (AI 对话/   │  │  (PostgreSQL  │
   │   宣言生成)   │  │   PG 模式)    │
   └──────────────┘  └──────────────┘
                            ▲
                            │ 服务端 REST 中转（不直连浏览器）
                            │ 隐私：仅存 4 个公开字段、无身份
```

> ⏳ AI 生图（开场配图）：`JIMENG_*` 环境变量预留但未接入，当前开场为纯文字 + 主题样式。

---

## 2. 技术选型（实际落地）

| 层 | 候选方案 | 实际选择 | 理由 |
|---|---|---|---|
| **前端框架** | Next.js / Vue / 纯HTML | **Next.js 14** | 自带前后端，新手友好，部署简单 |
| **后端** | Next.js 内置 / Flask / Express | **Next.js API Routes** | 不用单独搭后端，一套搞定 |
| **AI 大模型** | DeepSeek / 通义千问 / GPT-4 | **DeepSeek**（OpenAI 接口兼容） | 中文强、成本低；不填 key 走演示模式 |
| **数据库** | Supabase（已弃用）/ CloudBase | **腾讯云 CloudBase（PG 模式）** | supabase.co 国内不可达，改走 CloudBase REST |
| **卡片图生成** | 服务端渲染 / 前端 Canvas | **前端 Canvas**（1080×1350 PNG） | 本地生成、可下载/复制，无需额外服务 |
| **本地存储** | localStorage | **localStorage** | 对话状态、我的宣言本地留存 |
| **部署** | Vercel / CloudBase 云托管 / EdgeOne | **EdgeOne Pages** | 国内可直连，域名形如 `*.edgeone.cool` |
| **认证** | Supabase Auth（已弃用） | **无**（画廊匿名写入，无身份字段） | 隐私优先，降低合规复杂度 |

---

## 3. 项目目录结构（实际）

```
portfolio-project/
├── app/
│   ├── page.tsx                      # 首页 /
│   ├── chat/page.tsx                # 对话页 /chat（3 轮引导）
│   ├── gallery/page.tsx             # 宣言画廊 /gallery（我的 + 公共）
│   ├── about/page.tsx               # 关于页（产品复盘）
│   ├── api/chat/route.ts            # 聊天 API（限流 / 阶段校验 / 演示降级）
│   ├── api/generate-candidates/route.ts  # 生成 3 条宣言候选
│   └── api/gallery/route.ts         # 画廊 API（服务端中转 CloudBase）
├── components/
│   ├── ChatWindow.tsx               # 对话主状态机（核心）
│   ├── PublicGallery.tsx            # 公共画廊（云端）
│   ├── GalleryList.tsx              # 我的宣言（本地）
│   ├── DeclarationCard.tsx          # 宣言卡片
│   └── ...
├── lib/
│   ├── ai/                          # DeepSeek 客户端 + 角色 prompt + 演示降级
│   │   ├── deepseek.ts
│   │   ├── fallback.ts
│   │   └── prompts.ts               # 查拉图斯特拉角色 + 三轮引导 + 候选生成
│   ├── db/
│   │   ├── cloudbase.ts             # CloudBase 客户端（PG 模式 REST 封装）
│   │   └── declarations.ts          # 云端读写封装（浏览器侧 fetch）
│   ├── utils/                       # 卡片图生成 / 本地存储
│   │   ├── cardImage.ts
│   │   └── storage.ts
│   └── api/guard.ts                 # 限流 / 校验 / 错误处理（服务端）
├── cloudbase-fn/child-game/server.js  # （可选）CloudBase 云函数
├── docs/                            # 安装 / 部署 / 面试话术
├── PRD.md / 架构.md / progress.md   # 产品 / 技术 / 进度文档
├── edgeone.json                     # EdgeOne Pages 构建配置
└── .env.local.example               # 环境变量模板（安全，可提交）
```

---

## 4. 关键技术决策记录 (ADR)

### 决策 1：为什么选 Next.js
全栈一体、文件路由清晰、API Routes 简单、免费部署（EdgeOne Pages / Vercel 均支持）。

### 决策 2：为什么用 CloudBase 而不是 Supabase
原方案 Supabase 的 `supabase.co` 域名在中国大陆经常不可达，国内打不开。公共画廊改为走 **CloudBase PG 模式**（基于 PostgREST，原生 fetch 即可，无需 SDK）。鉴权用 Publishable Key（anon 角色），配合 RLS 实现匿名读写；表结构不含任何用户身份字段，天然保护隐私。

### 决策 3：画廊为什么走服务端中转、不直接连库
浏览器直连需要「安全域名白名单」，而 EdgeOne 每次预览部署域名都变，白名单跟不上。服务端 `/api/gallery` 中转后，域名随便变都不影响画廊。

### 决策 4：AI Prompt 设计思路
角色设定（查拉图斯特拉）+ 阶段引导（greet→0→1→2）+ 输出纪律约束（禁动作描写 / 禁 markdown / 固定 JSON 格式）。详见 `lib/ai/prompts.ts`。

---

## 5. 第三方依赖清单

| 依赖 | 用途 | 成本 |
|---|---|---|
| Next.js + Tailwind CSS | 全栈框架 + 样式 | 免费 |
| DeepSeek API | 对话 / 宣言生成 | 按调用付费（不填 key 走演示模式） |
| 腾讯云 CloudBase（PG 模式） | 公共画廊数据库 | 按量计费（有免费额度） |
| Canvas（浏览器原生） | 宣言卡片图导出 | 免费 |

---

## 6. 环境变量（实际）

```env
# —— CloudBase（公共画廊，仅服务端）——
CLOUDBASE_ENV_ID=child-game-xxxx
CLOUDBASE_PUBLISHABLE_KEY=your_publishable_key
# CLOUDBASE_PUBLISHABLE_KEY_1/2/3=   # 超长 key 分片（EdgeOne 单变量上限 1000 字符时备用）

# —— DeepSeek（AI 大模型）——
DEEPSEEK_API_KEY=sk-xxxxx
DEEPSEEK_BASE_URL=https://api.deepseek.com

# —— AI 生图（预留，未接入）——
# JIMENG_API_KEY=
# JIMENG_BASE_URL=
```
